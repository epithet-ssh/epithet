SetEnv TERM=xterm-256color

Match host v0004 exec "epithet match --host '%h' --port '%p' --user '%r' --jump '%j' --hash '%C'"
    IdentityAgent ~/.epithet/agent/%C
    PubkeyAuthentication yes
    PreferredAuthentications publickey
