# Example epithet client configuration
# Place in ~/.epithet/ as config.yaml or config.json

# Host patterns are obtained dynamically from CA discovery - no static match config needed
agent:
  ca-url: https://ca.example.com/
  auth: epithet auth oidc --issuer https://accounts.google.com --client-id abc123 --client-secret xyz098
