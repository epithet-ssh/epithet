openapi: 3.0.3
info:
  title: Epithet Policy Server API
  version: 7.0.0
  description: |
    The Policy Server API is called by the Epithet CA to make authorization decisions
    for SSH certificate requests. Your policy server receives normalized authentication
    and inventory facts plus connection details, and returns
    authorization limits if the request is approved. CA constructs the certificate.

    This OpenAPI specification can be used to:
    - Generate server stubs in your preferred language
    - Configure API gateways (AWS API Gateway, Kong, etc.)
    - Validate requests and responses
    - Generate client libraries

    ## Security

    Every request carries a short-lived JWT the CA mints per
    request and signs with its own SSH private key (`pkg/serviceauth`). Your policy
    server MUST verify this token (signature, audience, freshness, body hash, method,
    and target) before processing the request. There is no unauthenticated variant of
    this route.

  contact:
    name: Epithet Project
    url: https://github.com/epithet-ssh/epithet
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0

servers:
  - url: https://policy.example.com
    description: Your policy server endpoint

paths:
  /:
    post:
      operationId: evaluatePolicy
      summary: Evaluate certificate request policy
      description: |
        Evaluate whether to approve an SSH certificate request and determine
        the allowed TTL, extensions, and any optional absolute policy deadline.

        The policy server should:
        1. Verify the CA's service JWT (see Security)
        2. Require unexpired normalized authentication from inventory
        3. Bind user.id to authentication.id and target to the normalized connection.remoteHost
        4. Check authorization (can this identity access this host as the requested user?)
        5. Return a positive TTL, permitted extensions, policy content ID, and optionally a tighter absolute deadline

        CA derives exactly one principal and the certificate Key ID from its original
        inventory facts. Expiry is min(signing time + ttlSeconds seconds, authentication.expiresAt,
        optional notAfter). Non-200 responses never authorize signing.
        CA validates inventory host/domain binding before projection. Policy sees
        the requested target and the policy host resource; a shared domain name
        may intentionally differ from the target. Principal mode/domain and
        directory/host revisions remain private CA data.

      security:
        - ServiceJWT: []

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyRequest'

      responses:
        '200':
          description: Certificate request approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyResponse'
              examples:
                approved:
                  summary: Approved with standard permissions
                  value:
                    ttlSeconds: 300
                    policyId: "sha256:compiled-policy-content"
                    extensions:
                      permit-pty: ""
                      permit-agent-forwarding: ""

        '400':
          description: Malformed request body (not valid JSON, or the read itself failed)
          content:
            text/plain:
              schema:
                type: string
              example: "Invalid JSON: unexpected end of JSON input"

        '401':
          description: Invalid service JWT
          content:
            text/plain:
              schema:
                type: string
              example: "request verification failed"

        '403':
          description: User not authorized for this access
          content:
            text/plain:
              schema:
                type: string
              example: "User alice not authorized for deploy@prod-web-01.example.com"

        '405':
          description: Method not allowed (only POST is supported on this route)
          content:
            text/plain:
              schema:
                type: string
              example: "Method not allowed"

        '413':
          description: Request body exceeded the 64 KiB limit (`wire.MaxBodySize`)
          content:
            text/plain:
              schema:
                type: string
              example: "Request body too large"

        '500':
          description: Internal server error
          content:
            text/plain:
              schema:
                type: string
              example: "failed to marshal response: ..."

components:
  schemas:
    PolicyRequest:
      type: object
      required:
        - connection
        - facts
      properties:
        facts:
          $ref: '#/components/schemas/PolicyFacts'
        connection:
          $ref: '#/components/schemas/Connection'

    PolicyFacts:
      type: object
      additionalProperties: false
      required: [authentication, target, user, host]
      description: |
        Normalized facts projected by CA after validating inventory resolution.
        No inventory protocol version, timestamp, revisions, or principal metadata.
        Explicit null user/host means absent and leads to structural denial;
        omission is malformed. CA's service JWT binds all facts and the connection.
      properties:
        authentication:
          $ref: '#/components/schemas/Authentication'
        target:
          type: string
          minLength: 1
          description: Must equal connection.remoteHost after ASCII case folding
        user:
          $ref: '#/components/schemas/User'
        host:
          $ref: '#/components/schemas/HostResource'

    Authentication:
      type: object
      required: [id, expiresAt]
      properties:
        id:
          type: string
          minLength: 1
          description: Inventory-mapped, verified user ID
        expiresAt:
          type: string
          format: date-time
          description: Verified session expiry; must be in the future, and bounds certificate validity
    User:
      type: object
      nullable: true
      required: [id, userName, active]
      properties:
        id:
          type: string
          minLength: 1
        userName:
          type: string
          minLength: 1
        active:
          type: boolean
        userType:
          type: string
        groups:
          type: array
          nullable: true
          items:
            type: string
            minLength: 1
          description: Group membership identifiers matched byte-for-byte
        department:
          type: string
        organization:
          type: string
    HostResource:
      type: object
      nullable: true
      required: [name, accounts]
      properties:
        name:
          type: string
          minLength: 1
          description: Requested host, or named shared principal domain
        labels:
          type: object
          nullable: true
          additionalProperties:
            type: string
        accounts:
          type: array
          nullable: true
          items:
            type: string
          description: Must be present; null means ungrounded and [] permits no accounts

    Connection:
      type: object
      required:
        - remoteHost
        - remoteUser
        - port
        - hash
      properties:
        remoteHost:
          type: string
          description: Target SSH server hostname (OpenSSH %h)
          example: "prod-web-01.example.com"

        remoteUser:
          type: string
          description: |
            Target username on remote server (OpenSSH %r). Policy authorizes
            this account; CA encodes it as the certificate's sole principal
            using the inventory principal mode/domain retained at CA.
          example: "deploy"

        port:
          type: integer
          format: uint32
          description: Target SSH port (OpenSSH %p)
          example: 22
          minimum: 1
          maximum: 65535

        proxyJump:
          type: string
          description: ProxyJump configuration (OpenSSH %j), empty string if not used
          example: ""

        hash:
          type: string
          description: |
            OpenSSH %C hash - unique identifier for this connection, computed
            by OpenSSH from the full connection tuple.
          example: "a1b2c3d4e5f67890"

    PolicyResponse:
      type: object
      description: |
        HTTP 200 authorizes this connection under these limits. Policy does not
        return user identity or principals. CA constructs those from trusted
        inventory and connection facts and independently enforces authentication
        expiry, active-user, host, and account restrictions.
      required:
        - ttlSeconds
        - extensions
      properties:
        ttlSeconds:
          type: integer
          format: int64
          minimum: 1
          maximum: 9223372036
          description: |
            Maximum certificate lifetime in whole seconds,
            measured from CA signing, not evaluation. Must be positive. CA
            further clamps expiry to authentication.expiresAt and any notAfter.
            SSH timestamps have whole-second precision; an interval leaving
            no usable lifetime after truncation is rejected. The upper limit
            prevents overflow when converting seconds into Go time.Duration.
            Built-in policy rounds fractional deployment defaults down and rejects
            results below one second. Strings and fractional numbers are invalid.
          example: 300
        extensions:
          type: object
          description: |
            SSH certificate extensions to grant, such as permit-pty,
            permit-agent-forwarding, permit-port-forwarding, permit-user-rc,
            and permit-X11-forwarding. Values are typically empty strings.
            An empty map grants no extensions.
          additionalProperties:
            type: string
          example:
            permit-pty: ""
        notAfter:
          type: string
          format: date-time
          description: |
            Optional absolute policy deadline (RFC 3339). This can tighten but
            never extend the authentication expiry independently enforced by CA.
            Omit (or send the zero time) for no additional policy deadline.
            Expired deadlines reject issuance. Built-in Writ policy omits this;
            it does not echo authentication expiry or reinterpret Writ until.
          example: "2026-09-11T17:00:00Z"
        policyId:
          type: string
          description: Content ID of the compiled policy, for private issuance audit
  securitySchemes:
    ServiceJWT:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        A JWT the CA mints per request and signs with its own SSH private key,
        sent as `Authorization: Bearer <jwt>`. Claims:

        | Claim | Meaning |
        |---|---|
        | iss | CA identity: SHA256 fingerprint of the CA's SSH public key |
        | aud | `"epithet-policy"` |
        | iat / exp | Issued-at / expiry, ~60 seconds apart |
        | jti | Random token ID |
        | bh | base64url (raw, unpadded) of sha256(request body); empty-string body for GET |
        | htm | HTTP method |
        | htu | Request target: host + path |

        The signing algorithm is derived from the CA's key type
        (ed25519→EdDSA, RSA→PS256, ECDSA→ES256/ES384). `htm`/`htu` binding
        prevents a captured token from being replayed against a different
        method or path that happens to share the same body hash (e.g. an
        empty body on both a GET and a POST). Your implementation MUST
        verify this token — signature, audience, freshness, and body hash —
        before processing any request; there is no unauthenticated mode.

security:
  - ServiceJWT: []
