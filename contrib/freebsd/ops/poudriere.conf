# Installed as /usr/local/etc/poudriere.d/poudriere.conf on the build host.
ZPOOL=zroot
ZROOTFS=/poudriere
FREEBSD_HOST=https://download.FreeBSD.org
RESOLV_CONF=/etc/resolv.conf
BASEFS=/usr/local/poudriere
POUDRIERE_DATA=/usr/local/poudriere/data
DISTFILES_CACHE=/usr/ports/distfiles

# Candidate repository policy. The publisher signs the final Epithet-only
# repository on the host; the key is not mounted into poudriere build jails.
ATOMIC_PACKAGE_REPOSITORY=yes
COMMIT_PACKAGES_ON_FAILURE=no
KEEP_OLD_PACKAGES=yes
KEEP_OLD_PACKAGES_COUNT=5
PKG_REPRODUCIBLE=yes
PRESERVE_TIMESTAMP=yes
