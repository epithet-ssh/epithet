# match is repeatable, so both of these should make it in
match m0001
match m0002

# this will be dropped as there is another ca-url
ca-url http://skife.org/

# last one wins for non-repeatable things
ca-url http://xn.io/

# auth command is required
auth /usr/local/bin/epithet-auth
